import { Send } from "lucide-react";

export default function ApplyNowButton() {
    return (
        <button className="btn btn-primary w-full cursor-pointer whitespace-nowrap">
            <Send className="mr-2 size-4" /> Apply Now
        </button>
    );
}
